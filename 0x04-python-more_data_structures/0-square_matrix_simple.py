#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
	 new_matrix = []
    c = 0
    for i in matrix:
        new = matrix[c].copy()
for j in range(len(X[0])):
       result[i][j] = X[i][j] + Y[i][j]

for r in result:
   print(r)
